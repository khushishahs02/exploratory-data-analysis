# Copy this to .env.local for local dev (Vite reads it automatically)
# Leave empty to use the Vite proxy (localhost:8000)
VITE_API_URL=

# For production — set this in your Vercel environment variables:
# VITE_API_URL=https://your-render-app.onrender.com
